

git clone link of repo (HTTPS LINK of code changes)

git status: command to get information of repos changes

git add --all or git add -A: is the command to add all changes and remember it in git in the entire project

git reset: it is used to undo changes in git add

git add . :it modified changes in the file that are only in the perticular folder or file

git add * : only remembers the only new fils or modified not deleted onces

git add*.txt/.py/.c/etc : this staged only perticular type of files

Staging is the between step from locally work done to working git memory.

***for first time 
enter your email and username
***git config --global user.email email@gmai.command***
git config --global user.name "user-name"***


git rm file.txt : to delete the file

git reset --hard : the change or deleted get reset or come back

fit rm -f file.txt : the file is forceffully deleted

git em --chached file.txt :removes it from Staging

git rm -r folder : 

git log : full comment list when and which comment we have created

git log --oneline : short summary of comments

***Branching***
instead of making direct changes on the main branch git make branch to test them before merging it to main branch
secure and organised way to contribute

git branch : to see in which branch you are

git branch branchname :to create new branch in the project
git checkout branchname : to get into the created branch.

git merge main -m "merging main into main branch" :merging the things to main branch or another branch in the github

git comment (coment no.) :it takes you to the place where comment is done